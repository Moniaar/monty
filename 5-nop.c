#include "monty.h"

/**
 * nop_opcode - A function that bascally does nothing
 *
 * @stack: a pointer to a pointer pointing to top node of the stack
 *
 * @line_number: integer to tell you the line number of the given opCode
 *
 * Return: Void (Nothing)
 */

void nop_opcode(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
